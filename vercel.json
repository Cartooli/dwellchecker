{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install",
  "functions": {
    "app/api/ingestion/jobs/[id]/process/route.ts": { "maxDuration": 60 }
  }
}
